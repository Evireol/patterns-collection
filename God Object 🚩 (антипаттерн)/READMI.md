Исправленная версия "Божественного объекта"
-------------------

### Принцип единственной ответственности (SRP)

UserValidator отвечает только за валидацию.
UserRepository — только за сохранение/чтение.
NotificationService — только за отправку уведомлений.
UserRegistrationService — только за оркестрацию процесса регистрации.

### Паттерн «Сервис» (Service Layer / Application Service)

Класс UserRegistrationService — это сервис уровня приложения, который:

Координирует взаимодействие между другими компонентами,
Реализует одну бизнес-операцию (регистрация),
Не содержит состояния (stateless),
Является точкой входа для use-case'а.
Это часть архитектурных подходов вроде Domain-Driven Design (DDD) или Clean Architecture.

### Паттерн «Репозиторий» (Repository)

UserRepository скрывает детали доступа к данным (БД, API, файл и т.д.) за единым интерфейсом.
Это позволяет:

Менять СУБД без изменения бизнес-логики,
Легко мокать в тестах.
👉 Это классический GoF-совместимый паттерн, часто используемый в enterprise-разработке.

### Композиция вместо **монолита**

Вместо одного «всемогущего» класса — набор маленькиых, слабосвязанных компонентов, которые компонуются в нужный сервис.
Это следствие принципа «Favor composition over inheritance» (тоже из GoF).

### Внедрение зависимостей (Dependency Injection, DI)

Зависимости (UserRepository, Validator и др.) передаются извне через конструктор, а не создаются внутри.
Это:

Упрощает тестирование (можно подставить моки),
Снижает связанность,
Позволяет использовать DI-контейнеры (например, Symfony DI, Laravel Container, PHP-DI).

